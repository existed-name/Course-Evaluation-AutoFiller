直接问AI即可

这下面只是一小部分提示词，大部分内容都需要自己实操过后发现问题，然后再拷打AI

---

先问大概内容
```java
我想知道，JavaMaven项目怎么连同所有配置依赖打成一个可执行文件，然后发给其他人下载使用🤔（听说可以jar包，但是我在想怎么处理依赖、特别是单独下载的EdgeDriver）
```

```java
1、我的项目依赖：
（1）Maven项目，依赖都在`pom.xml`了

（2）只有EdgeDriver安装在特定目录，在代码中指定该目录（绝对路径，是不是可以移到项目里面来变成相对路径🤔另外EdgeDriver只需要它的.exe文件即可？我看他附带的Driver_Notes文件夹里面有1个credits.html、2个无后缀的说明文档EULA和LICENSE，应该不需要吧🤔）

2、针对用户：无Java环境、非技术人员

3、需要你教我怎么把项目弄成直接可运行的App🧐
```

然后根据他的回答, (开新对话)问细节

```java
maven-shade-plugin跟maven-assembly-plugin有什么区别👀
那么最终打Jar包（包含依赖）有多少种推荐操作、最终选择📕
```

```java
我的Maven项目用到了EdgeDriver，项目中指定它的绝对路径，但是如果要打成用户用的App肯定不行

于是我在想（除了替换为轻量级的配置放进pom.xml之后在线下载配置）可不可以把EdgeDriver（msedgedriver.exe)放进项目特定文件夹，
比如说src/main/resources下面，然后使用相对路径`src/main/resources/msedgedriver.exe`，就可以一起打包了🧐

主要还是这个路径问题👀
```

```java
哦对了🤔
我突然想起来jar好像必须要Java环境才可以运行，如果对方没有Java环境该怎么操作🤔
现在我已经把代码 + pom.xml + resources的Driver合并到FatJar里面了，那么现在只需要FatJar + JRE？？？怎么操作呢🧐

是不是还有这样：代码 + pom.xml + resources（不包括需要下载的外部软件，比如EdgeDriver） => jar，然后 + JRE + 外部软件，一起打成JPackage？？

详细讲解👀
```

打好FatJar后打JPackage
```jav
搞成.exe之类的发给其他人直接用，通常是FatJar + JRE => JPackage？？？🤔

FatJar我倒是打出来了，但是JRE怎么操作😂然后又怎么打JPackage？？

详细讲解👀
```

---

